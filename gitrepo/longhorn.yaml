apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: longhorn
  namespace: fleet-local
spec:
  branch: main
  paths:
    - /system/longhorn
    - /system/longhorn/storageclass
  paused: false
  repo: https://github.com/fuscoyu/fleet-homelab.git